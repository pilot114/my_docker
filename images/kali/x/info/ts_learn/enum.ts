enum Membership {
    Simple,
    Standard,
    Premium,
}

const membership = Membership.Standard
const membershipName = Membership[1]

enum SocialMedia {
    VK = 'VK',
    FACEBOOK = 'FACEBOOK',
}

const media = SocialMedia.VK
